name: qFoldIT Conformance

on:
  pull_request:
  push:
    branches: [main]

jobs:
  adapter:
    uses: qfoldit/.github/.github/workflows/reusable-qfoldit-conformance.yml@main
    with:
      manifest_path: qfoldit.adapter.json
      require_manifest: true
